# EJEMPLO DE CONFIGURACIÓN PARA PRODUCCIÓN
# Copia este archivo a .env y ajusta los valores según tu entorno

# ============================================
# CONFIGURACIÓN DE SERVIDOR
# ============================================

# URL completa de tu aplicación en producción
# Ejemplo: http://192.168.1.100:8080 o https://tudominio.com
NEXTAUTH_URL="http://localhost:3000"

# Puerto en el que correrá el servidor (para server.js)
PORT=3000

# Hostname del servidor
HOSTNAME="localhost"

# Entorno de ejecución
NODE_ENV=production

# ============================================
# BASE PATH (si tu app está en un subdirectorio)
# ============================================
# Ejemplo: Si tu app está en http://servidor.com/reebok-app
# NEXT_PUBLIC_BASE_PATH=/reebok-app
NEXT_PUBLIC_BASE_PATH=

# ============================================
# SEGURIDAD
# ============================================

# Clave secreta para sesiones (genera una nueva para producción)
# Puedes generarla con: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
NEXTAUTH_SECRET="SitioReebokRetail2025"

# ============================================
# BASE DE DATOS SAP HANA
# ============================================
HANA_CONNECTION_STRING="DRIVER=HDBODBC;UID=SYSTEM;PWD=TU_PASSWORD;ServerNode=servidor:30015;DATABASE=SBOVECTOR;"

# ============================================
# BASE DE DATOS SQL SERVER (Prisma)
# ============================================
DATABASE_URL="sqlserver://servidor:1433;database=Soporte;user=Admin;password=PASSWORD;encrypt=true;trustServerCertificate=true"

# SQL Server - Conexión directa (para mssql library)
DB_HOST=servidor
DB_PORT=1433
DB_NAME=Soporte
DB_USER=Admin
DB_PASSWORD=PASSWORD
DB_ENCRYPT=true
DB_TRUST_CERT=true

# ============================================
# API EXTERNA (CodigosIC)
# ============================================
EXTERNAL_API_USER=UsrWhlsReebok
EXTERNAL_API_PASSWORD=TU_PASSWORD_API

# External API Base URL and Endpoints
EXTERNAL_API_BASE_URL=https://www.vectordelta.com.mx:81
EXTERNAL_API_CLIENTES_ENDPOINT=/UnionGroup/API/Query/MasterData/ClientesParaCodigosIC
EXTERNAL_API_CODIGOS_IC_ENDPOINT=/UnionGroup/API/Insert/dd/CodigosIC

# ============================================
# USUARIO ADMINISTRADOR (para seed script)
# ============================================
ADMIN_EMAIL=admin@uniongroup.mx
ADMIN_PASSWORD=TU_PASSWORD_ADMIN
ADMIN_USERNAME=ADMINISTRADOR
